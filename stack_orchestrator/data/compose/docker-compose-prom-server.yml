version: "3.7"

services:
  prometheus:
    image: prom/prometheus
    restart: always
    volumes:
      - ../config/metrics/prometheus:/etc/prometheus
      # TODO: persist data
      # - prometheus_data:/prometheus
    ports:
      - "9090:9090"
